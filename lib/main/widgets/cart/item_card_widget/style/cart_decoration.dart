import 'package:flutter/material.dart';

final BoxDecoration cartDecoration = BoxDecoration(
    borderRadius: BorderRadius.circular(30),
    color: Colors.white70,
    boxShadow: [
      BoxShadow(
          color: Colors.grey.withOpacity(0.2),
          offset: const Offset(0, 4),
          blurRadius: 10)
    ]);
